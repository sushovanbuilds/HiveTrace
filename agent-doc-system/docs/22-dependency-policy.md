# Dependency Policy

Before adding a dependency, assess:

1. Does the standard library or existing dependency solve it?
2. Is the package maintained?
3. What is its security posture?
4. What license does it use?
5. What is its runtime/build cost?
6. Is it compatible with the current stack?
7. Can it be removed later?

Agents must justify non-trivial new dependencies.
